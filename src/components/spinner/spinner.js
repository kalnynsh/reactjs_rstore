import React from 'react';
import './spinner.css';

const Spinner = () => {

    return (
        <div className="loading-spinner">
            ... loading
        </div>
    );
};

export default Spinner;
